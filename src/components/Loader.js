import React from 'react';
import './Loader.css'; // Assuming you have a CSS file for styles

const Loader = () => {
  return (
    <div className="loader">
      <p>Loading...</p>
    </div>
  );
};

export default Loader;
